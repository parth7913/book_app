import 'package:get/get.dart';

class homeModal {
  String? name,other, image, Rating, Publish_Year,id;

  homeModal(
      {required this.name,
        required this.other,
        required this.image,
        required this.Rating,
        required this.Publish_Year,
        required this.id});
}